version: 2.1
jobs:
  test:
    docker:
      - image: circleci/node:lts
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Install dependency
          command: npm install
      - run:
          name: Run test
          command: npm run test