apiVersion: skaffold/v1beta16
kind: Config
metadata:
  name: portfolio-dev
build:
  artifacts:
  - image: portfolio-dev
    context: ../
    docker:
        dockerfile: Dockerfile.dev
deploy:
  kubectl:
    manifests:
    - portfolio-dev-deploy.yaml
    - portfolio-dev-service.yaml
